pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git 'https://github.com/ShivaChouhan/devops_training.git'
            }
        }

        stage('List Project Files') {
            steps {
                bat 'dir'
            }
        }

        stage('Serve HTML Page') {
            steps {
                bat '''
                echo Starting Python web server on port 8000...
                cd Jenkis_Assignment1
                powershell -Command "Start-Process python -ArgumentList '-m','http.server','8000'"
                '''
                echo '✅ HTML Calculator should be available at: http://localhost:8000/calculator.html'
            }
        }
    }
}
